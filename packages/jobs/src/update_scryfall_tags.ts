// The Scryfall tag-data pipeline (oracle tags + illustration tags) was removed
// along with the otag/atag filter and Oracle/Art Tags sort features. This file
// is left as a no-op so the existing npm scripts (update-scryfall-tags /
// dev:update-scryfall-tags) don't blow up on stale invocations.
console.info('update_scryfall_tags is a no-op — Scryfall tag features were removed.');
